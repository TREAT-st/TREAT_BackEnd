spring:
  datasource:
    url: jdbc:mysql://localhost:3306/treat?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ${SPRING_DATABASE_USERNAME}
    password: ${SPRING_DATABASE_PASSWORD}

